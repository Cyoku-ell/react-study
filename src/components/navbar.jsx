function Navbar() {
  return (
    <nav>
      <h2>Rafael's porto</h2>

      <div>
        <a href="#">Home</a>
        <a href="#">About</a>
        <a href="#">Projects</a>
      </div>
    </nav>
  );
}

export default Navbar;
